<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Team Crystal — Free Fire Squad</title>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
<style>
/* --- GLOBAL --- */
* { margin:0; padding:0; box-sizing:border-box; font-family:'Poppins',sans-serif; scroll-behavior:smooth; }
body { background:#0b0b0f; color:#fff; line-height:1.6; }
a { text-decoration:none; }

/* NAVIGATION */
nav { background:#111; padding:16px 0; position:sticky; top:0; z-index:100; box-shadow:0 2px 15px #00000055; }
nav .container { display:flex; justify-content:center; gap:32px; }
nav a { font-weight:600; color:#fff; transition:0.2s; }
nav a:hover { color:#00ffff; }

/* HERO */
.hero { text-align:center; padding:160px 20px; }
.hero h1 { font-size:4rem; color:#00ffff; margin-bottom:12px; }
.hero p { font-size:1.5rem; color:#ccc; margin-bottom:30px; }
.btn { display:inline-block; padding:16px 40px; margin:8px; font-size:1rem; font-weight:700; border-radius:12px; background: linear-gradient(90deg,#00ffff,#9b59b6); color:#000; transition:0.3s; }
.btn:hover { background: linear-gradient(90deg,#00cccc,#8e44ad); }

/* SECTIONS */
section { padding:80px 20px; opacity:0; transform:translateY(50px); transition:1s ease; }
section.visible { opacity:1; transform:translateY(0); }
h2 { text-align:center; font-size:2.5rem; margin-bottom:18px; color:#00ffff; }

/* FEATURES */
.features { display:flex; gap:20px; justify-content:center; flex-wrap:wrap; margin-top:20px; }
.feature-card { background:#111; padding:24px; width:260px; border-radius:12px; text-align:center; box-shadow:0 0 15px #00ffff33; transition:0.3s; }
.feature-card:hover { transform:translateY(-8px); box-shadow:0 0 25px #00ffff; }
.feature-card h3 { margin-bottom:10px; font-size:1.3rem; color:#00ffff; }
.feature-card p { color:#ccc; font-size:0.95rem; }

/* TEAM */
.team-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(180px,1fr)); gap:18px; margin-top:20px; }
.member { background:#111; padding:18px; border-radius:10px; text-align:center; transition:0.3s; }
.member:hover { transform:translateY(-8px); box-shadow:0 0 20px #00ffff; }
.member h3 { color:#fff; margin-bottom:4px; }
.member p { color:#aaa; font-size:0.9rem; }

/* APPLY FORM */
.apply-form { background:#111; max-width:500px; margin:0 auto; padding:30px; border-radius:12px; display:flex; flex-direction:column; gap:12px; box-shadow:0 0 20px #00ffff33; }
.apply-form input, .apply-form textarea { width:100%; padding:14px; border:none; border-radius:6px; background:#222; color:#fff; }
.apply-form input[type="submit"] { background: linear-gradient(90deg,#00ffff,#9b59b6); color:#000; font-weight:700; cursor:pointer; transition:0.3s; }
.apply-form input[type="submit"]:hover { background: linear-gradient(90deg,#00cccc,#8e44ad); }

/* FOOTER */
.footer { text-align:center; margin:40px 0; font-size:0.9rem; color:#888; }

/* ANIMATIONS */
@keyframes fadeDown { from {opacity:0; transform:translateY(-50px);} to {opacity:1; transform:translateY(0);} }
@keyframes fadeUp { from {opacity:0; transform:translateY(50px);} to {opacity:1; transform:translateY(0);} }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="container">
    <a href="#about">About</a>
    <a href="#features">Features</a>
    <a href="#team">Team</a>
    <a href="#apply">Apply</a>
    <a href="#contact">Contact</a>
  </div>
</nav>

<!-- HERO -->
<div class="hero container">
  <h1>Team Crystal</h1>
  <p>Free Fire Elite Squad — Compete, Train & Win!</p>
  <a href="#apply" class="btn">Apply Now</a>
  <a href="https://discord.gg/pVQDeUbz9U" target="_blank" class="btn">Join Discord</a>
</div>

<!-- ABOUT -->
<section id="about" class="container">
  <h2>About Team Crystal</h2>
  <p style="text-align:center; max-width:900px; margin:12px auto; color:#ccc;">
    Team Crystal is a professional Free Fire squad focused on strategy, coordination, and victory. Join our elite team and take your skills to the next level.
  </p>
</section>

<!-- FEATURES -->
<section id="features" class="container">
  <h2>Why Join Us</h2>
  <div class="features">
    <div class="feature-card"><h3>Elite Gameplay</h3><p>Play with highly skilled members and improve your performance.</p></div>
    <div class="feature-card"><h3>Training Sessions</h3><p>Regular practice sessions to sharpen your skills.</p></div>
    <div class="feature-card"><h3>Tournaments</h3><p>Compete in tournaments and showcase your talent.</p></div>
    <div class="feature-card"><h3>Community</h3><p>Be part of an active, friendly Discord community.</p></div>
  </div>
</section>

<!-- TEAM -->
<section id="team" class="container">
  <h2>Our Team</h2>
  <div class="team-grid">
    <div class="member"><h3>Leader</h3><p>Player1</p></div>
    <div class="member"><h3>Sniper</h3><p>Player2</p></div>
    <div class="member"><h3>Support</h3><p>Player3</p></div>
    <div class="member"><h3>Rusher</h3><p>Player4</p></div>
  </div>
</section>

<!-- APPLY -->
<section id="apply" class="container">
  <h2>Apply to Join</h2>
  <form id="applyForm" class="apply-form">
    <input type="text" id="name" placeholder="Full Name" required>
    <input type="text" id="ign" placeholder="In-Game Name (IGN)" required>
    <input type="text" id="discord" placeholder="Discord / Social ID" required>
    <textarea id="reason" placeholder="Why should we choose you?" rows="4" required></textarea>
    <input type="submit" value="Submit Application">
  </form>
</section>

<!-- CONTACT -->
<section id="contact" class="container" style="text-align:center;">
  <h2>Contact</h2>
  <p>Discord: <a href="YOUR_DISCORD_INVITE_LINK" target="_blank">Join Server</a></p>
  <p>Email: teamcrystal@example.com</p>
</section>

<!-- FOOTER -->
<div class="footer">&copy; 2025 Team Crystal • Clean & Modern Design</div>

<!-- JS -->
<script>
// Scroll animations
const sections = document.querySelectorAll("section");
window.addEventListener("scroll", () => {
  const triggerBottom = window.innerHeight / 5 * 4;
  sections.forEach(section => {
    if(section.getBoundingClientRect().top < triggerBottom) section.classList.add("visible");
  });
});

// Discord webhook form
const webhookURL = "https://discord.com/api/webhooks/1455848943803171003/QRlL6IevGeWEydcNa6pe2Tm0Xd92IbvZWH1P3o0-rV5qCIATHnw7PaxqRksMVgNMu8lv";
document.getElementById('applyForm').addEventListener('submit', e => {
  e.preventDefault();
  const name = document.getElementById('name').value;
  const ign = document.getElementById('ign').value;
  const discord = document.getElementById('discord').value;
  const reason = document.getElementById('reason').value;
  fetch(webhookURL, {
    method: 'POST',
    headers: {'Content-Type':'application/json'},
    body: JSON.stringify({content:`**New Application**\n**Name:** ${name}\n**IGN:** ${ign}\n**Discord:** ${discord}\n**Reason:** ${reason}`})
  })
  .then(res => res.ok ? alert("Application sent!") : alert("Failed!"))
  .catch(err => { console.error(err); alert("Error!"); });
  e.target.reset();
});
</script>

</body>
</html>
